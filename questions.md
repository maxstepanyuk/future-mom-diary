1. ServerApi routes
    Auth pages сделан через [AuthType] как делать routes для Auth(login, register - public) (session logout - private) разная логика запросов, для private надо передавать cookie

2. CSS Container

3. Модлаки перехватываются или нет

4. Методата для всех страниц?

5. 

